<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rando.mstoton.improvject">
    >
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Black.NoTitleBar">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TeamNamesActivity" />
        <activity android:name=".PassTeam1Activity" />
        <activity android:name=".SecretObjectActivity" />
        <activity android:name=".GameSetupActivity" />
        <activity android:name=".GameplayActivity" />
        <activity android:name=".ScoringActivity" />
        <activity android:name=".FillerActivity" />
        <activity android:name=".EndActivity" />
        <activity android:name=".ScoringResultActivity" />
        <activity android:name=".PassTeam2Activity" />
        <activity android:name=".MixUpActivity" />
        <activity android:name=".RulesActivity"></activity>
    </application>

</manifest>